import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('DetailsPokemonPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(
    // DetailsPokemonPage(title: 'DetailsPokemon')));
    //  final titleFinder = find.text('DetailsPokemon');
    //  expect(titleFinder, findsOneWidget);
  });
}
