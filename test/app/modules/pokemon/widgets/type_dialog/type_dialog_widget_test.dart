void main() {
//  testWidgets('TypeDialogWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(TypeDialogWidget()));
//    final textFinder = find.text('TypeDialog');
//    expect(textFinder, findsOneWidget);
//  });
}
