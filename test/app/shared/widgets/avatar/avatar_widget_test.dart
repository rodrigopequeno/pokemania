void main() {
//  testWidgets('AvatarWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(AvatarWidget()));
//    final textFinder = find.text('Avatar');
//    expect(textFinder, findsOneWidget);
//  });
}
